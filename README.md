dot-files
=========

My dot files.

These are managed using the (chezmoi)[https://chezmoi.io] package.

Download and run `install.sh` on a fresh machine.
Then run the `boostrap.sh` script to ensure the packages are loaded and machine is ready.
